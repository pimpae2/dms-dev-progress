# ประวัติเครื่อง Dev

1. ใน Apps Script เดิม เพิ่มไฟล์ชื่อ `dev-history` แล้วใส่โค้ดจาก `apps-script/dev-history.gs` โดยไม่ลบโค้ด UAT
2. บันทึก แล้วเลือกฟังก์ชัน `setupDevHistory` และกดเรียกใช้ อนุญาตสิทธิ์ด้วยบัญชีที่อ่านชีตต้นทางได้
3. เปิดลิงก์ History ในบันทึกการดำเนินการ จะพบไฟล์ใหม่ `DEV Progress History` และแท็บ `DEV-History`
4. เผยแพร่เฉพาะแท็บ DEV-History เป็น CSV เมื่อได้รับอนุญาตให้เผยแพร่ข้อมูลชุดนี้ ลิงก์เผยแพร่ทำให้บุคคลภายนอกที่มีลิงก์อ่านข้อมูลได้
5. นำ URL CSV ใส่ `DEV_HISTORY_CSV_URL` ใน `project-progress.js` แล้ว deploy เว็บตามปกติ

สคริปต์อ่านต้นทางเท่านั้น และเก็บประวัติทุกชั่วโมงในไฟล์ใหม่ของผู้รัน แยกจาก UAT ไม่ต้องเปิดคอมพิวเตอร์ทิ้งไว้ ตรวจสอบ trigger `captureDevHistory` และประวัติการทำงานหากข้อมูลไม่เพิ่ม

เว็บเทียบข้อมูลล่าสุดกับ snapshot สุดท้ายของเมื่อวานตามเวลาไทย ต้องมีประวัติเมื่อวานจริงจึงจะเปรียบเทียบได้ วันแรกแสดงว่ายังไม่มีข้อมูล ไม่สร้างข้อมูลย้อนหลังขึ้นเอง หน้าเว็บอ่านใหม่เมื่อเปิดหน้าหรือกดรีเฟรช ไม่ได้รีเฟรชอัตโนมัติเมื่อเปิดค้างไว้

ข้อมูลที่เก็บคือเวลา วันที่ รหัสงาน เครื่อง ชื่องาน และสถานะเท่านั้น ผู้รับผิดชอบใน popup ใช้ข้อมูลปัจจุบันจากชีตต้นทาง

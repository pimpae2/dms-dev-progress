{
  "name": "dms-dev-progress",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@netlify/blobs": "^10.5.0"
  },
  "engines": {
    "node": ">=22"
  }
}
